'use strict'; const log =console.log; (async ()=>{

type langNum = [string, string, number];
let x: langNum, y: langNum;

x = ['zero', 'cero', 0    ];
y = ['one' , 1,    , 'uno'];

log(x);
log(y);

})();